# MATCHa

MATCHa is a semantic schema matching algorithm designed to combine different matchers to get the best results.

To run a simple demo, clone the repository, install requirements.txt and run run.py